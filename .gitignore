node_modules

.next/

# Local env — only truly-local secrets. .env.production holds public
# NEXT_PUBLIC_* Firebase values needed by the Cloudflare build, so it is
# tracked. firestore.rules is source-of-truth for security rules and is
# tracked too.
.env
.env.local
.env*.local

# build/type-check caches - no secrets, just churn
tsconfig.tsbuildinfo
*.tsbuildinfo

# OS / editor cruft
.DS_Store
.vscode/
.idea/

# Cloudflare local state
.wrangler/
.dev.vars
